---
description: Конвенции MOVAŬ — модульный монолит, русский UI, английский код
alwaysApply: true
---

# MOVAŬ

- Код: C# PascalCase, TypeScript camelCase. JSON API — snake_case. Комментарии кратко на русском.
- Имена в API: `help_request`. В UI: «заявка».
- Не добавляй микросервисы, пока домен не потребует отдельного деплоя.
- UI: `design-system/MOVAU/MASTER.md`. Иконки Lucide, без emoji. Кириллица в шрифтах.
- Заявки хранят PostGIS `Geography(POINT, 4326)`: WKT `POINT(lng lat)`.
